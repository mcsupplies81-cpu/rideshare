import { NextResponse } from 'next/server';import { createServiceClient } from '@/lib/supabase/server';import { requireAdminApi } from '@/lib/admin';
export async function GET(req: Request){const auth=await requireAdminApi();if(auth.error)return auth.error;const u=new URL(req.url);const status=u.searchParams.get('status');const search=u.searchParams.get('search');const page=Number(u.searchParams.get('page')??1);const limit=Number(u.searchParams.get('limit')??20);const from=(page-1)*limit;const supabase=await createServiceClient();let q=supabase.from('drivers').select('id,approval_status,is_online,total_rides,created_at,user:users!drivers_id_fkey(full_name,phone),driver_plans(plan_type,is_active)',{count:'exact'}).order('created_at',{ascending:false}).range(from,from+limit-1);if(status&&status!=='all')q=q.eq('approval_status',status);if(search)q=q.or(`id.ilike.%${search}%`);const {data,count,error}=await q;if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data,page,limit,total:count??0});}
