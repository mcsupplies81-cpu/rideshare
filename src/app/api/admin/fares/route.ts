import { NextResponse } from 'next/server';import { createServiceClient } from '@/lib/supabase/server';import { requireAdminApi } from '@/lib/admin';
export async function GET(){const a=await requireAdminApi();if(a.error)return a.error;const s=await createServiceClient();const [{data:fare},{data:pricing}] = await Promise.all([s.from('fare_settings').select('*').order('effective_from',{ascending:false}).limit(1).maybeSingle(),s.from('vehicle_pricing').select('*').order('vehicle_type')]);return NextResponse.json({fare_settings:fare,vehicle_pricing:pricing??[]});}
export async function POST(req:Request){const a=await requireAdminApi();if(a.error)return a.error;const b=await req.json();const s=await createServiceClient();await s.from('fare_settings').insert({base_fare:b.base_fare,per_mile_rate:b.per_mile_rate,minimum_fare:b.minimum_fare,per_minute_rate:0,effective_from:new Date().toISOString()});return NextResponse.json({success:true});}
