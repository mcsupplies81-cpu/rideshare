import { NextResponse } from 'next/server';import { createServiceClient } from '@/lib/supabase/server';import { requireAdminApi } from '@/lib/admin';
export async function POST(_:Request,{params}:{params:Promise<{driverId:string}>}){const auth=await requireAdminApi();if(auth.error)return auth.error;const {driverId}=await params;const supabase=await createServiceClient();const {count:docs}=await supabase.from('driver_documents').select('*',{count:'exact',head:true}).eq('driver_id',driverId);if((docs??0)<3)return NextResponse.json({error:'Driver missing required documents'},{status:400});await supabase.from('drivers').update({approval_status:'approved'}).eq('id',driverId);const {count}=await supabase.from('drivers').select('*',{count:'exact',head:true}).eq('approval_status','approved');await supabase.from('driver_plans').update({is_active:false}).eq('driver_id',driverId);await supabase.from('driver_plans').insert({driver_id:driverId,plan_type:(count??0)<=100?'trial':'per_ride',is_active:true,ends_at:(count??0)<=100?new Date(Date.now()+30*86400000).toISOString():null});return NextResponse.json({success:true});}
