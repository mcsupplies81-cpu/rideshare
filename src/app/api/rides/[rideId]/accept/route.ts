import { NextResponse } from 'next/server'; import { createClient, createServiceClient } from '@/lib/supabase/server'; import { stripe } from '@/lib/stripe/client'
export async function POST(_req:Request,{params}:{params:Promise<{rideId:string}>}){const {rideId}=await params; const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) return NextResponse.json({error:'unauthorized'},{status:401}); const {data:ride}=await supabase.from('rides').select('*').eq('id',rideId).single(); if(ride?.status!=='searching') return NextResponse.json({error:'Ride no longer available'},{status:409}); const {data:driver}=await supabase.from('drivers').select('approval_status,is_online,stripe_connect_onboarded').eq('id',user.id).single(); if(driver?.approval_status!=='approved' || !driver.is_online || !driver.stripe_connect_onboarded) return NextResponse.json({error:'driver_not_ready'},{status:403}); const service=await createServiceClient(); await service.from('rides').update({status:'accepted',driver_id:user.id,accepted_at:new Date().toISOString()}).eq('id',rideId).eq('status','searching'); if(ride.stripe_payment_intent_id) await stripe.paymentIntents.capture(ride.stripe_payment_intent_id); await service.from('ride_events').insert({ride_id:rideId,event_type:'driver_accepted',actor_id:user.id}); return NextResponse.json({success:true})}
