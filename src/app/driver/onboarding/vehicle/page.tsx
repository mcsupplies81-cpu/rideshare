'use client'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
export default function VehicleStep(){const r=useRouter(); const [form,setForm]=useState({make:'',model:'',year:2020,color:'',license_plate:'',vehicle_type:'base'}); async function submit(e:FormEvent){e.preventDefault(); await fetch('/api/drivers/vehicle',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}); localStorage.setItem('driver_onboarding_step','3'); r.push('/driver/onboarding')} return <form onSubmit={submit} className="space-y-3">{['make','model','color'].map(k=><input key={k} required placeholder={k} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="w-full rounded border p-2"/>)}<input type="number" min={2010} max={2025} value={form.year} onChange={e=>setForm({...form,year:Number(e.target.value)})} className="w-full rounded border p-2"/><input required placeholder="License plate" value={form.license_plate} onChange={e=>setForm({...form,license_plate:e.target.value.toUpperCase()})} className="w-full rounded border p-2"/><div>{[['base','Standard'],['smooth','Smooth'],['xl','XL']].map(([v,l])=><label key={v} className="mr-4"><input type="radio" checked={form.vehicle_type===v} onChange={()=>setForm({...form,vehicle_type:v})}/> {l}</label>)}</div><button className="rounded bg-purple-600 px-4 py-2 text-white">Continue</button></form> }
