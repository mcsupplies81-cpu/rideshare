import { createClient } from '@/lib/supabase/server'
export default async function ProfilePage(){const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) return null; const [{data:u},{data:v}] = await Promise.all([supabase.from('users').select('full_name,phone').eq('id',user.id).single(), supabase.from('vehicles').select('make,model,year,license_plate').eq('driver_id',user.id).maybeSingle()]); return <main className='space-y-2'><h1 className='text-2xl font-semibold'>Account</h1><p>{u?.full_name}</p><p>{u?.phone}</p>{v && <p>{v.year} {v.make} {v.model} · {v.license_plate}</p>}<form action='/auth/signout' method='post'><button className='rounded border px-4 py-2'>Sign Out</button></form></main>}
