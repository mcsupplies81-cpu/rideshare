'use client'
import Link from 'next/link'
import { useParams, useRouter } from 'next/navigation'
import { useEffect, useState } from 'react'
export default function TripPage(){const {rideId}=useParams<{rideId:string}>(); const r=useRouter(); const [ride,setRide]=useState<any>(null); useEffect(()=>{const load=()=>void fetch(`/api/rides/${rideId}/status`).then(x=>x.json()).then(setRide); load(); const i=setInterval(load,3000); return ()=>clearInterval(i)},[rideId]); if(!ride) return <p>Loading...</p>; const post=async(action:string)=>{await fetch(`/api/rides/${rideId}/${action}`,{method:'POST'}); if(action==='complete') r.push('/driver')}; if(ride.status==='accepted') return <main className='space-y-3'><h1 className='text-2xl font-semibold'>En route to pickup</h1><p className='text-lg'>{(ride.rider_name||'Rider').split(' ')[0]} · {ride.pickup_address}</p><a href={`https://maps.google.com/?daddr=${ride.pickup_lat},${ride.pickup_lng}`} className='inline-block rounded border px-4 py-2'>Open in Google Maps</a><button onClick={()=>post('arrived')} className='w-full rounded bg-purple-600 p-3 text-white'>Arrived at Pickup</button></main>; if(ride.status==='driver_arrived') return <main className='space-y-3'><h1 className='text-2xl font-semibold'>Rider is waiting</h1><p>{ride.rider_name||'Rider'}</p><button onClick={()=>post('start')} className='w-full rounded bg-purple-600 p-3 text-white'>Start Trip</button></main>; if(ride.status==='in_trip') return <main className='space-y-3'><h1 className='text-2xl font-semibold'>Trip in progress</h1><p>{ride.dropoff_address}</p><p>Estimated fare: ${Number(ride.estimated_fare||0).toFixed(2)}</p><button onClick={()=>post('complete')} className='w-full rounded bg-purple-600 p-3 text-white'>Complete Trip</button></main>; return <main className='space-y-3'><h1 className='text-2xl font-semibold'>Trip Complete! ✓</h1><Link href='/driver' className='inline-block rounded border px-4 py-2'>Back to Dashboard</Link></main> }
